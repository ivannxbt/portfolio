# ------------------------------------------------------------
# Frontend/public variables (safe to expose to the browser)
# ------------------------------------------------------------
NEXT_PUBLIC_SITE_URL=https://your-domain.com
NEXT_PUBLIC_SUBSTACK_USERNAME=your_substack_username
# Optional when frontend and backend are deployed separately.
# Leave empty to use same-origin /api routes.
NEXT_PUBLIC_API_BASE_URL=

# ------------------------------------------------------------
# Backend/secret variables (server-only)
# ------------------------------------------------------------
# Admin credentials
ADMIN_EMAIL=you@example.com
ADMIN_PASSWORD_HASH=your_bcrypt_hashed_password   # generate with: node generate-hash.mjs

# NextAuth
NEXTAUTH_SECRET=your_secure_random_string          # generate with: openssl rand -base64 32

# AI features — required for /api/chat (Google Gemini)
GOOGLE_API_KEY=your_google_api_key

# Database (optional — reserved for future use)
# DATABASE_URL=postgresql://user:pass@host/db
